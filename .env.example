# Neon Postgres 接続文字列
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# CryptoCompare 価格API
CRYPTOCOMPARE_API_KEY=

# Vercel Cron 認証(Vercel Cron が Authorization: Bearer <値> を自動付与)
CRON_SECRET=

# Basic認証(全ルートに適用。/api/cron/* は除外)
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
